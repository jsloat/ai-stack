# Done Feature Docs

This directory holds feature docs whose primary implementation phases are complete.

These files remain useful as design history and reference material, but they should not be treated as the default active backlog when planning the next task.

When deciding what to work on next:

- check active docs in `docs/features/` first
- consult `docs/features/done/` when you need historical context, completed contracts, or implementation rationale
