# Copy this file to config.local.yaml and fill in values for your environment.
# Keep config.local.yaml untracked.

defaultHarness: copilot
yolo: false

models:
  planner: sonnet
  implementer: gpt-5.5
  cheapVerifier: gpt-5.5-mini

telemetry:
  enabled: true

orchestration:
  # A machine-local path set in config.local.yaml, for example:
  # root: ~/projects
  root:

repos:
  # The machine-local checkout path for ai-stack, for example:
  # aiStack: ~/Dev/ai-stack
  aiStack:
