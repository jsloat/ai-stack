# Copy this file to config.local.yaml and fill in values for your environment.
# Keep config.local.yaml untracked.

defaultHarness: copilot
yolo: false

models:
  planner: sonnet
  implementer: gpt-5.5
  cheapVerifier: gpt-5.5-mini

telemetry:
  enabled: true
