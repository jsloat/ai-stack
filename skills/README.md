# Skills

This directory holds reusable task-shaped instructions and assets.

Structure:

- `shared/` contains skills that are safe to commit and distribute
- `local/` contains private or environment-specific skills that may exist locally but should not become part of the shared repo contract by default

Skills are not the place for broad repository policy. Durable repo-wide guidance belongs in shared docs and instruction files.
